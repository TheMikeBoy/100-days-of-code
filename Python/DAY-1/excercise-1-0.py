num_twodigits = input("write a two digits number: ")

str_twodigits=str(num_twodigits)


print ( str(str_twodigits[0]) + "+" + str(str_twodigits[1]), int(str_twodigits[0]) + int(str_twodigits[1]) )

# I chose to use a variable to store the value of the convert integer, but is not necessary, you can use something like this str(num_twodigits)[1]

# math operations

# + add
#- sub 
#* multiplicatoin
#/ division
#** power
#()Paranteses
#Order matters between division and multiliplication is the left to the rigth 